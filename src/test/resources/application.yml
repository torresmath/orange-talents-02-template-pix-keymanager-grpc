micronaut:
  application:
    name: key-manager-grpc-test

api.participant: 60701190

datasources:
  default:
    url: jdbc:h2:mem:pix_key_manager_test
    driverClassName: org.h2.Driver
    username: root
    password: ""
    schema-generate: CREATE_DROP
    dialect: H2

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop